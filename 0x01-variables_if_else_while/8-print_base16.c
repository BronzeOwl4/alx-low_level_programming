#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 12; i < 22; i++)
	{
		printf("%d", i);
	}
	char ch;
	for (ch = 'a'; ch < 'g'; ch++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
