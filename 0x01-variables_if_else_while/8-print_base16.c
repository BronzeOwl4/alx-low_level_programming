#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	char ch;
	for (ch = 'a'; ch < 'g'; ch++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
