#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 12; i < 22; i++)
	{
		putchar(i);
	}
	for (i = 60; i < 65; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
