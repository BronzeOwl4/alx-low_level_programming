#include <stdio.h>
#include "main.h"
/**
 * main - Entry point
 *
 * Return: Always 0 (Success(
 */
int main(void)
{
	int i = 3;
	int a = 1, b = 2, c;

	printf("%d, %d", a, b);
	while (i <= 98)
	{
		c = a + b
			printf(", %d", c);
		a = b;
		b = c;
		i++;
	}
	printf("\n");
	return (0);
}
