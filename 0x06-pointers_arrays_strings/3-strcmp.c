#include <string.h>
#include <stdio.h>
#include "main.h"

/**
 * _strcmp - compares two strings
 * @s1: string one
 * @s2: string two
 *
 * Return: Always 0
 */
int _strcmp(char *s1, char *s2)
{
	int result = strcmp(s1, s2);
	return (result);
}
